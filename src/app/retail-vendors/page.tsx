export default function RetailVendorsPage() {
  return (
    <div className="max-w-2xl mx-auto px-4 sm:px-6 py-16 text-center">
      <div className="text-4xl mb-4">🤝</div>
      <h1 className="text-xl font-semibold mb-2">Retail Vendors</h1>
      <p className="text-muted max-w-md mx-auto">
        This is where you&rsquo;ll track vendors who buy on credit and settle up every
        week or two — a live running balance instead of the bahi khata. Coming in the
        next phase.
      </p>
    </div>
  );
}
